{
  "name": "smart-rental-platform",
  "version": "1.0.0",
  "description": "Rental marketplace for stays, tools, and services",
  "scripts": {
    "dev": "tailwindcss -i ./assets/styles/app.css -o ./public/css/app.css --watch",
    "build": "tailwindcss -i ./assets/styles/app.css -o ./public/css/app.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
