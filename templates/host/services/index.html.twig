{% extends 'base.html.twig' %}

{% block title %}My Services{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>My Services</h1>
    <a href="{{ path('host_service_new') }}" class="btn btn-primary">Add New Service</a>
</div>

<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Duration</th>
                <th>Location</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for service in services %}
                <tr>
                    <td>{{ service.name }}</td>
                    <td>€{{ service.basePrice }}</td>
                    <td>{{ service.durationMinutes }} min</td>
                    <td>{{ service.location }}</td>
                    <td>
                        {% if service.isActive %}
                            <span class="badge bg-success">Active</span>
                        {% else %}
                            <span class="badge bg-warning">Pending</span>
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('host_service_edit', {id: service.id}) }}" class="btn btn-sm btn-warning">Edit</a>
                        <form method="post" action="{{ path('host_service_delete', {id: service.id}) }}" style="display:inline;">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure?')">Delete</button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="text-center">No services yet. Create your first one!</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
