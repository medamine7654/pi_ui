{% extends 'base.html.twig' %}

{% block title %}Create Tool - Host Dashboard{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-6">
    <div class="max-w-2xl mx-auto">
        {# Back button #}
        <div class="mb-6">
            <a href="{{ path('host_tools') }}" class="inline-flex items-center text-sm text-gray-600 hover:text-gray-900">
                <svg class="h-4 w-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                </svg>
                Back to My Tools
            </a>
        </div>

        {# Page Header #}
        <div class="mb-6">
            <h1 class="text-3xl font-bold text-gray-900">Add New Tool</h1>
            <p class="text-gray-600 mt-1">List a new tool for rent</p>
        </div>

        {# Form Card #}
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            {{ form_start(form) }}
                <div class="space-y-6">
                    <div>
                        {{ form_label(form.name, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                        {{ form_widget(form.name, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent'}}) }}
                        {{ form_errors(form.name) }}
                    </div>

                    <div>
                        {{ form_label(form.description, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                        {{ form_widget(form.description, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent', 'rows': 4}}) }}
                        {{ form_errors(form.description) }}
                    </div>

                    <div>
                        {{ form_label(form.category, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                        {{ form_widget(form.category, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent'}}) }}
                        {{ form_errors(form.category) }}
                        <p class="mt-1 text-sm text-gray-500">Select the category that best describes your tool</p>
                    </div>

                    <div>
                        {{ form_label(form.pricePerDay, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                        {{ form_widget(form.pricePerDay, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent'}}) }}
                        {{ form_errors(form.pricePerDay) }}
                        
                        {# AI Price Suggestion #}
                        <div id="price-suggestion-tool" class="mt-3 hidden">
                            <div class="flex items-start gap-2 p-3 bg-gradient-to-r from-purple-50 to-pink-50 border border-purple-200 rounded-lg">
                                <i class="fas fa-magic text-purple-600 mt-0.5"></i>
                                <div class="flex-1">
                                    <p class="text-sm font-medium text-gray-900">
                                        Suggested Price: <span id="suggested-price-tool" class="text-purple-600 font-bold">€0</span>
                                    </p>
                                    <p class="text-xs text-gray-600 mt-1">
                                        Based on <span id="similar-count-tool">0</span> similar tools in this category
                                    </p>
                                    <button type="button" id="use-suggested-price-tool" class="mt-2 text-xs text-purple-600 hover:text-purple-800 font-medium">
                                        Click to use this price →
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div>
                        {{ form_label(form.stockQuantity, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                        {{ form_widget(form.stockQuantity, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent'}}) }}
                        {{ form_errors(form.stockQuantity) }}
                    </div>

                    <div>
                        {{ form_label(form.imageFile, 'Tool Image', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                        {{ form_widget(form.imageFile, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent'}}) }}
                        {{ form_errors(form.imageFile) }}
                        <p class="mt-1 text-sm text-gray-500">Max 5MB. Formats: JPG, PNG, GIF</p>
                    </div>

                    <div class="bg-blue-50 border border-blue-200 rounded-md p-4">
                        <div class="flex">
                            <svg class="h-5 w-5 text-blue-400 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                            </svg>
                            <p class="text-sm text-blue-700">Your tool will be pending until approved by an administrator.</p>
                        </div>
                    </div>

                    <div class="flex gap-3 pt-4">
                        <button type="submit" class="flex-1 px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 font-medium transition-colors">
                            Create Tool
                        </button>
                        <a href="{{ path('host_tools') }}" class="px-4 py-2 border border-gray-300 text-gray-700 rounded-md hover:bg-gray-50 font-medium transition-colors">
                            Cancel
                        </a>
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Get form elements by querySelector to handle Symfony's generated IDs
    const categorySelect = document.querySelector('select[name="tool[category]"]');
    const priceInput = document.querySelector('input[name="tool[pricePerDay]"]');
    const suggestionBox = document.getElementById('price-suggestion-tool');
    const suggestedPriceSpan = document.getElementById('suggested-price-tool');
    const similarCountSpan = document.getElementById('similar-count-tool');
    const useButton = document.getElementById('use-suggested-price-tool');
    
    // Pre-calculated price suggestions from server
    const priceSuggestions = {{ priceSuggestions|json_encode|raw }};
    
    console.log('Tool Price Suggestions:', priceSuggestions);
    console.log('Category Select:', categorySelect);
    console.log('Price Input:', priceInput);
    console.log('Suggestion Box:', suggestionBox);
    
    let currentSuggestion = null;
    
    // Listen for category changes
    if (categorySelect) {
        categorySelect.addEventListener('change', function() {
            const categoryId = this.value;
            console.log('Category changed to:', categoryId);
            
            if (!categoryId) {
                suggestionBox.classList.add('hidden');
                return;
            }
            
            fetchPriceSuggestion(categoryId);
        });
    }
    
    // Click to use suggested price
    if (useButton) {
        useButton.addEventListener('click', function() {
            if (currentSuggestion) {
                priceInput.value = currentSuggestion;
                priceInput.focus();
            }
        });
    }
    
    function fetchPriceSuggestion(categoryId) {
        const suggestion = priceSuggestions[categoryId];
        console.log('Fetching suggestion for category:', categoryId, 'Result:', suggestion);
        
        if (suggestion && suggestion.count > 0) {
            currentSuggestion = suggestion.suggested;
            suggestedPriceSpan.textContent = '€' + suggestion.suggested;
            similarCountSpan.textContent = suggestion.count;
            suggestionBox.classList.remove('hidden');
            console.log('Showing suggestion:', suggestion);
        } else {
            suggestionBox.classList.add('hidden');
            console.log('No suggestion available');
        }
    }
});
</script>
{% endblock %}
